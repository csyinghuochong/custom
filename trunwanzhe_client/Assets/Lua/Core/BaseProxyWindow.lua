local mLuaClass = require "Core/LuaClass"
local mBaseViewFunction = require "Core/BaseViewFunction"
local mColor = Color;
local BaseProxyWindow = mLuaClass("BaseProxyWindow",mBaseViewFunction);

return BaseProxyWindow;